<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_add_user"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_grey"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:focusableInTouchMode="true"
    tools:context="com.zeiss.koch.kaffeekasse.RegisterUserActivity">

    <TextView
        android:id="@+id/registerUserTextView"
        style="@style/TextAppearance.Huge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Nutzer registrieren" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_weight="4">

        <ListView
            android:id="@+id/userList"
            android:layout_width="350dp"
            android:layout_height="match_parent"
            android:layout_gravity="start"
            android:cacheColorHint="#00000000"
            android:dividerHeight="1px"
            android:background="#AAAAAA"
            android:alpha="200"
            android:choiceMode="singleChoice"/>

        <Button
            android:id="@+id/registerUserButton"
            android:layout_width="wrap_content"
            android:layout_height="100dp"
            android:layout_margin="16dp"
            android:layout_gravity="center_vertical"
            android:layout_weight="2"
            android:onClick="RegisterUserClick"
            android:text="Gescannten Ausweis Für den ausgewählten Nutzer registrieren " />
    </LinearLayout>
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end|center_vertical"
        android:layout_weight="1">
        <Button
            android:id="@+id/openAddUserButton"
            android:layout_width="200dp"
            android:layout_height="50dp"
            android:layout_gravity="center_vertical"
            android:onClick="OpenAddUserClick"
            android:textSize="12dp"
            android:text="Neuen Nutzer hinzufügen " />

    </LinearLayout>



</LinearLayout>
