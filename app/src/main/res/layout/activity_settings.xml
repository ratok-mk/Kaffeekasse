<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_settings"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.zeiss.koch.kaffeekasse.SettingsActivity"
    android:orientation="vertical"
    android:weightSum="1"
    android:descendantFocusability="beforeDescendants"
    android:focusableInTouchMode="true"
    android:background="@color/background_grey">


    <include layout="@layout/activity_settings_admin"
        android:visibility="visible"
        android:id="@+id/adminLayout" />

    <include layout="@layout/activity_settings_treasurer"
        android:visibility="gone"
        android:id="@+id/treasurerLayout" />

    <Button
       android:text="Anwendung beenden"
       android:layout_width="match_parent"
       android:layout_height="wrap_content"
       android:id="@+id/exitApplicationButton"
       android:onClick="ExitClick"
       android:layout_alignParentBottom="true"
       android:layout_alignParentStart="true" />
</RelativeLayout>